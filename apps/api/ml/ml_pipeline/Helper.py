import re
import pandas as pd


class Helper:
    def __init__(self):
        pass
    
    @staticmethod
    def clean_prices(self, prices: pd.DataFrame) -> pd.DataFrame:
        return prices
    
    @staticmethod
    def clean_news(self, news: pd.DataFrame) -> pd.DataFrame:
        return news

    @staticmethod
    def clean_sentiment(self, sentiment: pd.DataFrame) -> pd.DataFrame:
        return sentiment